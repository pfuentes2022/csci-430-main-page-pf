<!DOCTYPE html>
<html lang="eng">
    <head>
        {{<head}}
    </head>

    <body>
        {{>navbar}}

        

        <div class="container">
            <div class="row">
                <div class="col-5">
                    <h1>Error fetching page data</h1>
                </div>
            </div>
        </div>
        
    </body>

    {{>footer}}
</html>